/*
    Commands to install:
    node --version
    npm install -g tsx

    Command to run:
    tsx assignment.tsx
*/

const MAX_LIVES: number = 3;
let playerName: string = "Jordan";
let currentLives: number = MAX_LIVES;
let isGameOver: boolean = false;

// Print the player's name
console.log(playerName);

// Print current amount of lives
console.log(currentLives)

// Decrease lives by 1;
currentLives = currentLives - 1;

//Decrease lives by 1;
currentLives = currentLives - 1;

// What is the current amount of lives?
//console.log(currentLives);

// Is the game over?
//console.log(isGameOver);